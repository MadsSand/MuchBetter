
[project]
name = "muchbetter"
version = "0.1.0"
description = "Golf app for herreklub"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "psycopg[binary]",
    "python-dotenv",
]