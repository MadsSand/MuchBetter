[project]
name = "muchbetter"
version = "0.1.0"
description = "Golf app for herreklub"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0,<4",
    "gunicorn>=22",
    "psycopg[binary]>=3.1,<4",
    "python-dotenv>=1.0",
    "pillow>=10",
]

[tool.uv]
package = false
